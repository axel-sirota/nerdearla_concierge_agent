.venv/
__pycache__/

# Real keys never leave this machine. The template does get committed:
# the global gitignore blocks .env.* so it needs an explicit un-ignore.
.env
!.env.example
*.pyc
.ipynb_checkpoints/
progress.txt
.DS_Store

# Research plans and diagram sources stay local unless we decide to ship them
plans/

# Kryla tooling state
.kryla/
