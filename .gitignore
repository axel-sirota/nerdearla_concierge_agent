.venv/
__pycache__/

# Keys come from getpass now, so there is no .env in the workshop flow.
# This stays ignored for the local build venv, which does use one.
.env
*.pyc
.ipynb_checkpoints/
progress.txt
.DS_Store

# Research plans and diagram sources stay local unless we decide to ship them
plans/

# Kryla tooling state
.kryla/

# Generated by the notebook's %%writefile cell
servidor_serper.py
servidor_serper_v2.py
servidor_serper_roto.py
HANDOFF.md
mcp_servidor.log
